version: '3.9'

services:
  web:
    build: .
    container_name: inventory_web
    command: gunicorn core.wsgi:application --bind 0.0.0.0:8000
    volumes:
      - static_volume:/app/staticfiles
      - media_volume:/app/media
      - db_volume:/app/db
    env_file:
      - .env
    ports:
      - "8000:8000"   # 👈 important (expose to host)
    restart: unless-stopped

volumes:
  static_volume:
  media_volume:
  db_volume: